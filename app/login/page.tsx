'use client';import {useState}from'react';import axios from'axios';import Button from '@/components/ui/Button';import Card from '@/components/ui/Card';
export default function Login(){const[phone,setPhone]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(){try{const r=await axios.post('/api/auth/login',{phone,password});localStorage.setItem('token',r.data.token);setMsg('Logged in successfully. Go to dashboard.')}catch(e:any){setMsg(e.response?.data?.error||'Login failed')}}return <Card className="mx-auto max-w-md"><h1 className="font-serif text-4xl text-eid-gold">Login</h1><input className="mt-6 w-full rounded-xl bg-white/10 p-3" placeholder="Phone" value={phone} onChange={e=>setPhone(e.target.value)}/><input className="mt-3 w-full rounded-xl bg-white/10 p-3" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><Button className="mt-5 w-full" onClick={submit}>Login</Button><p className="mt-4 text-sm text-white/70">{msg}</p></Card>}
